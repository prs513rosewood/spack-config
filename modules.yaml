modules:
  default:
    enable:
      - tcl
    tcl:
      hash_length: 0
      projections:
        all: '{name}/{version}'
      all:
        environment:
          set:
            '{name}_ROOT': '{prefix}'
