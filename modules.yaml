modules:
  default:
    roots:
      lmod: ~/lmod
    enable:
    - lmod
    lmod:
      hash_length: 0
      projections:
        all: '{name}/{version}'
      all:
        environment:
          set:
            '{name}_ROOT': '{prefix}'
